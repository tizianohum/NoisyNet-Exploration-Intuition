### Noise Net Exploration

### Setup
uv venv .noisynet --python 3.11 
source .noisynet/bin/activate (mac) | .noisynet\Scripts\activate (windows)
uv pip install -r requirements.txt

#new packages
uv pip freeze > requirements.txt
